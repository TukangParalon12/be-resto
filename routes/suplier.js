var express = require("express");
var router = express.Router();
const models = require("../models/index");
router.post("/add_sup",async(req,res)=>{
    try {
        
    } catch (error) {
        
    }
})

module.exports = router;
